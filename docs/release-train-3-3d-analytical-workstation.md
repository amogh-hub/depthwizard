# Release Train 3 — Production 3D Analytical Workstation

## Mission

Turn the persisted DepthWizard elevation project into a production 3D analytical workspace without
creating a second, scientifically divergent data path. The terrain renderer must consume the same
hashed DSM/rDSM and source RGB already recorded by the project manifest, while all numerical probes
continue to come from the persisted raster products rather than approximated mesh vertices.

## Integrated scope

- persistent textured GLB terrain generation from the project DSM/rDSM and original RGB;
- deterministic multi-resolution LOD pyramid with edge-preserving sampling;
- adaptive finest-LOD stride so large rasters do not create unbounded browser geometry;
- SHA-256 identity for the source texture, elevation surface, every GLB and mesh manifest;
- idempotent/resumable mesh build when the surface, texture and build configuration are unchanged;
- durable `mesh` stage and mesh artifacts in `project-manifest.json`;
- local API to build/reload the mesh report and stream a requested GLB LOD;
- desktop project mesh loading rather than demo-only terrain;
- explicit LOD selection and display-only vertical exaggeration;
- Orbit, Fly, First Person and Top Down navigation on the real project terrain;
- UV/raycast 3D picking mapped back to normalized raster coordinates;
- synchronized 3D probe values through the existing project-analysis API;
- measurement/profile workflows can use the same normalized coordinates from 2D or 3D;
- display exaggeration never mutates persisted elevation or analytical measurements;
- deterministic scientific ZIP export assembled only from hash-verified persisted artifacts;
- export manifest carrying per-member SHA-256, bytes, semantics and units;
- opt-in source-imagery inclusion so large/restricted inputs are not redistributed by default;
- mesh and validation products can be included as transport derivatives without changing evidence.

## Scientific and geospatial invariants

1. Terrain generation consumes only the persisted project surface and the original source RGB.
   Reference DSM, residual rasters and validation metrics are never inputs to mesh geometry.
2. The mesh builder verifies the recorded source/surface SHA-256 values before generating anything.
3. The GLB is a visualization derivative. Numerical values shown to an analyst come from the
   persisted raster products through the probe/profile subsystem, not from interpolating display
   geometry.
4. Metric DSM projects use physical horizontal GSD in metres. Relative/non-georeferenced projects
   retain pixel horizontal units and relative vertical units rather than inventing metres.
5. Nodata pixels remove terrain faces. Nodata values are sanitized only for unreferenced vertices so
   they cannot corrupt scene bounds; they are not turned into valid terrain.
6. Vertical exaggeration is a renderer transform only. The project DSM/rDSM, mesh manifest and
   analyst-reported elevations remain unchanged.
7. LOD switching may change displayed triangle density but not the underlying project coordinate
   contract or analytical sampling result.
8. A 3D click is converted through GLB UV coordinates to the same normalized image coordinate used
   by the 2D analytical workspace.
9. Terrain products are hash-addressed in the project manifest and revalidated when reloaded.
10. A mesh build failure is recorded as a mesh-stage failure and cannot rewrite reconstruction,
    calibration or validation evidence.
11. Scientific export packaging re-hashes every registered artifact before inclusion. A changed or
    missing registered product blocks export rather than silently packaging stale data.
12. Export packaging does not rerun reconstruction, calibration, validation or mesh generation and
    does not mutate the project manifest. It is a transport operation over existing evidence.
13. Source imagery is excluded from the default ZIP. Its SHA-256 remains in the project manifest; the
    bytes are included only when the analyst explicitly opts in.
14. ZIP member paths are generated by DepthWizard rather than copied from arbitrary filesystem
    paths, preventing project-relative path traversal or accidental absolute-path disclosure inside
    the archive.

## First vertical-slice acceptance — passed

The project-derived mesh slice passed both GitHub and local acceptance on the already-accepted
OrthoLoC engineering project. This is product/rendering acceptance, not new elevation-accuracy
validation.

Local evidence on head `51a3b3a6f894ad8243b79f1652ee6a972ada655d`:

- `make verify`: 109 tests passed; Ruff clean; Pyright 0 errors / 0 warnings;
- desktop TypeScript/Vite production build passed;
- persistent mesh acceptance completed without rerunning DA3 reconstruction;
- surface: metric DSM with metre XY and metre Z;
- valid terrain pixels: 1,048,576;
- scene relief: 28.123 m;
- LOD 0: stride 2, 263,169 vertices, 524,288 faces, 13.08 MiB;
- LOD 1: stride 4, 66,049 vertices, 131,072 faces, 4.82 MiB;
- LOD 2: stride 8, 16,641 vertices, 32,768 faces, 2.75 MiB;
- LOD 3: stride 16, 4,225 vertices, 8,192 faces, 2.23 MiB;
- every GLB header/hash and mesh-manifest registration was checked by the acceptance harness;
- project manifest preserved `reference_data_used: false` for mesh geometry.

GitHub CI on the same head passed Python 3.12, Python 3.13 and desktop frontend jobs.

## Second vertical slice — deterministic project export

RT3 now also carries a production export contract:

- `ProjectExportRequest` controls source, mesh and validation inclusion;
- every registered artifact is re-hashed immediately before packaging;
- `export-manifest.json` records project-manifest identity and every member hash/size/semantics/units;
- ZIP timestamps and archive ordering are fixed so an unchanged project/configuration produces a
  byte-identical bundle;
- source imagery is excluded by default and included only by explicit opt-in;
- service endpoints build/read/download the package;
- desktop API types/client functions are present for final UI wiring;
- automated tests cover determinism, source opt-in, tamper rejection and archive download;
- a dedicated RT3 export smoke reuses the accepted RT2/RT3 project and performs no inference.

The export bundle remains a transport derivative. It does not make a new accuracy claim and does not
promote any estimator.

## Train boundary

RT3 is not yet closed. Remaining work includes analytical raster overlays on terrain,
profile/measurement geometry drawn in 3D, camera bookmarks and deterministic flythrough paths,
automatic LOD policy/performance telemetry, desktop export interaction, mesh seam/UV checks on large
scenes, and standalone desktop reliability. Those capabilities continue to build on the persistent
project-mesh and hash-audited export contracts rather than creating demo-only paths.
